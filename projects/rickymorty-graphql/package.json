{
  "name": "rickymorty-graphql",
  "version": "1.0.0",
  "description": "",
  "main": "src/app.js",
  "scripts": {
    "build": "babel src --out-dir dist",
    "dev": "nodemon --watch src --exec 'npm run build && node dist/app.js'",
    "start": "npm run build && node dist/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.6.0",
    "@babel/core": "^7.6.0",
    "@babel/node": "^7.6.1",
    "@babel/preset-env": "^7.6.0",
    "nodemon": "^1.19.2"
  },
  "dependencies": {
    "graphql-yoga": "^1.18.3",
    "request": "^2.88.0"
  }
}
